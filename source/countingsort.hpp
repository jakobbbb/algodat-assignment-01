#ifndef COUNTINGSORT_HPP
#define COUNTINGSORT_HPP

#include <vector>

std::vector<unsigned> countingsort(std::vector<unsigned> const& xs);
std::vector<unsigned> countingsort(std::vector<unsigned> const& xs, unsigned k);

#endif  // COUNTINGSORT_HPP
